<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://arielsboiardi.eu/feed.xml" rel="self" type="application/atom+xml"/><link href="https://arielsboiardi.eu/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-03-25T06:30:36+00:00</updated><id>https://arielsboiardi.eu/feed.xml</id><title type="html">blank</title><subtitle>Personal webpage of Ariel Surya Boiardi, scientific nomad, mostly working in Applied Mathematics, Mechanics and Physics. </subtitle><entry><title type="html">Hydrogel wrinkles</title><link href="https://arielsboiardi.eu/blog/2025/Wrinkles/" rel="alternate" type="text/html" title="Hydrogel wrinkles"/><published>2025-05-13T18:00:00+00:00</published><updated>2025-05-13T18:00:00+00:00</updated><id>https://arielsboiardi.eu/blog/2025/Wrinkles</id><content type="html" xml:base="https://arielsboiardi.eu/blog/2025/Wrinkles/"><![CDATA[<p>These days I am developing a more reliable lithography process for making hydrogels. Unfortunately, today’s tests resulted in the samples not detaching from the substrate. On the bright side, some intriguing wrinkling patterns formed on the surface of the hydrogels as a result of constrained swelling.</p> <div align="center"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/width_fab_10x-480.webp 480w,/assets/img/width_fab_10x-800.webp 800w,/assets/img/width_fab_10x-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/width_fab_10x.png" class="rounded" width="50%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption"> Wrinkling patterns in hydrogels. The image shows a 10x magnification of the hydrogel surface. </div> </div>]]></content><author><name></name></author><category term="research"/><category term="hydrogel"/><category term="wrinkles"/><category term="materials"/><category term="science"/><summary type="html"><![CDATA[Nice wrinkling patterns in hydrogels]]></summary><media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="https://arielsboiardi.eu/assets/images/wrinkles.jpg"/><media:content medium="image" url="https://arielsboiardi.eu/assets/images/wrinkles.jpg" xmlns:media="http://search.yahoo.com/mrss/"/></entry><entry><title type="html">Arduino I2C controller for the TI DLPC350</title><link href="https://arielsboiardi.eu/blog/2024/Arduino_I2C_oscilloscope/" rel="alternate" type="text/html" title="Arduino I2C controller for the TI DLPC350"/><published>2024-12-11T00:00:00+00:00</published><updated>2024-12-11T00:00:00+00:00</updated><id>https://arielsboiardi.eu/blog/2024/Arduino_I2C_oscilloscope</id><content type="html" xml:base="https://arielsboiardi.eu/blog/2024/Arduino_I2C_oscilloscope/"><![CDATA[<p>In the last week I have been programming an Arduino Nano ESP32 to control a DLP projector over the I2C protocol. I post here some notes on this ongoing work.</p> <h3 id="premises">Premises</h3> <p>The projector is the <a href="https://www.ti.com/tool/WDST-3P-PRO4500#overview">Wintech PRO4500</a> we use at <a href="http://samba.sissa.it/">SAMBA Lab</a> for the grayscale photolithography of soft active materials such as polymer gels (as described e.g. in <a class="citation" href="#boiardi_minimal_2024">(Boiardi &amp; Noselli, 2024)</a>). This device is based on Texas Instrument’s DLP LightCrafter 4500 that utilizes the DLP4500 WXGA DMD (digital micromirror array) and Texas Instrument’s DLPC350 controller.</p> <p>The <a href="https://www.ti.com/lit/ug/dlpu010g/dlpu010g.pdf">technical documentation</a> from TI suggests two ways to interface with the projector: I2C and USB; I opted for I2C for simplicity, also considering that my project does not have high bandwidth requirements.</p> <p>The DLPC350 operates at 3.3V, as stated <a href="https://www.ti.com/lit/ug/dlpu011f/dlpu011f.pdf">here</a>, therefore the I2C connected needs to be driven with a 3.3V signal. For this reason, the Arduino Nano ESP32 was selected as a controller, as it natively operates at 3.3V.</p> <h3 id="the-i2c-protocol">The I2C protocol</h3> <p>The source for this section is a <a href="https://www.ti.com/lit/an/sbaa565/sbaa565.pdf">document</a> from Texas Instruments, which contains way more detail that needed for my project.</p> <p>I2C (Inter-Integrated Circuit) is a two-wire serial communication protocol using a serial data line (SDA) and a serial clock line (SCL). Multiple controllers and target devices can be simultaneously connected on the same I2C bus. Each target node is identified by a unique 7 bit address.</p> <p>The SCL line is primarily controlled by the controller device and is used to synchronously data on the network; multiple clock speeds are supported. The serial data line SDA is used to transmit data to or from target devices. At a given time only one device can be transmitting data on the I2C bus.</p> <p>To avoid contention of the line, I2C communication is initiated from the controller device with an I2C START condition. To do this, the controller device first pulls the SDA low and then pulls the SCL low. This sequence indicates that the controller device is claiming the I2C bus for communication, forcing other controller devices on the bus to hold their communication. When the controller device has completed communication, the SCL releases high and then the SDA releases high. This indicates an I2C STOP condition.</p> <div align="center"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/posts/I2C_Arduino/START_STOP-480.webp 480w,/assets/posts/I2C_Arduino/START_STOP-800.webp 800w,/assets/posts/I2C_Arduino/START_STOP-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/posts/I2C_Arduino/START_STOP.png" class="rounded" width="50%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption"> I2C START and STOP [<a href="https://www.ti.com/lit/an/sbaa565/sbaa565.pdf" target="_blank">source Texas Instruments</a>] </div> </div> <p>After the communication has been initiated, a sequence of ones and zeros for serial communication. SDA is used for the data bits while SCL is the serial clock that times the bit sequence. A logical one is sent when the SDA releases the line, allowing the pullup resistor to pull the line to a high level. A logical zero is sent when SDA pulls down on the line, setting a low level near ground.</p> <p>The ones and zeros are received when SCL is pulsed. For a valid bit, SDA does not change between a rising edge and the falling edge of SCL for that bit. Changes of the SDA between the rising and falling edges of the SCL can be interpreted as a START or STOP condition on the I2C bus.</p> <p>The first data sent is the 7bit address of the target node, and the 8th bit of the first <em>frame</em> is used to indicate the mode. If this bit is 1, the controller is asking to read data from the target device. If this bit is 0, the controller asks to write data to the target device.</p> <p>After any communication byte, an extra 9th bit is used to verify the communication was successful. The target device pulls down the SDA during the SCL pulse to indicate to the controller that the address was received. This is known as an acknowledge (ACK) bit. If this bit is high, then no target device received the address and the communication was unsuccessful.</p> <p>All subsequent data is sent one byte at a time in data frames separated by ACK bits.</p> <h2 id="step-1-testing-the-i2c-signal">Step 1: Testing the I2C signal</h2> <p>While writing the code for the Arduino, I wanted to test the I2C signal, before connecting it to the projector. The testing was done with a second Arduino, acting as a target node as the projector, and reading the signal from with an oscilloscope (<a href="https://www.rohde-schwarz.com/products/test-and-measurement/oscilloscopes/rs-hmo1002-oscilloscope_63493-61541.html">R&amp;S®HMO1002</a>).</p> <h3 id="first-messages-through-i2c">First messages through I2C</h3> <p>Let us try to send a <code class="language-plaintext highlighter-rouge">0x00</code> byte over the I2C protocol, here is the code</p> <div class="language-c++ highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#include</span> <span class="cpf">&lt;Arduino.h&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;Wire.h&gt;</span><span class="cp">
</span>
<span class="cp">#define I2C_ADDRESS 0x1A
</span>
<span class="kt">void</span> <span class="nf">setup</span><span class="p">()</span> <span class="p">{</span>
  <span class="n">Wire</span><span class="p">.</span><span class="n">begin</span><span class="p">();</span> <span class="c1">// I2C0 </span>
<span class="p">}</span>

<span class="kt">void</span> <span class="nf">loop</span><span class="p">()</span> <span class="p">{</span>
  <span class="n">Wire</span><span class="p">.</span><span class="n">beginTransmission</span><span class="p">(</span><span class="n">I2C_ADDRESS</span><span class="p">);</span> <span class="c1">//begin transmit to device 0x1A</span>
  <span class="n">Wire</span><span class="p">.</span><span class="n">write</span><span class="p">(</span><span class="mh">0x00</span><span class="p">);</span> <span class="c1">//send data</span>
  <span class="n">Wire</span><span class="p">.</span><span class="n">endTransmission</span><span class="p">();</span> <span class="c1">//stop transmit</span>
  <span class="n">delay</span><span class="p">(</span><span class="mi">100</span><span class="p">);</span>
<span class="p">}</span>
</code></pre></div></div> <p>The message can be read from a reader arduino programmed with the following code:</p> <div class="language-c++ highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#include</span> <span class="cpf">&lt;Arduino.h&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;Wire.h&gt;</span><span class="cp">
</span>
<span class="cp">#define I2C_ADDRESS 0x1A
</span>
<span class="kt">void</span> <span class="nf">wireEvent</span><span class="p">(</span><span class="kt">int</span> <span class="n">howMany</span><span class="p">)</span> <span class="p">{</span>
  <span class="n">Serial</span><span class="p">.</span><span class="n">print</span><span class="p">(</span><span class="s">"Received on I2C: "</span><span class="p">);</span>
  <span class="k">while</span> <span class="p">(</span><span class="n">Wire</span><span class="p">.</span><span class="n">available</span><span class="p">())</span> <span class="p">{</span>
    <span class="n">byte</span> <span class="n">c</span> <span class="o">=</span> <span class="n">Wire</span><span class="p">.</span><span class="n">read</span><span class="p">();</span> <span class="c1">// receive byte as a character</span>
	<span class="n">Serial</span><span class="p">.</span><span class="n">print</span><span class="p">(</span><span class="n">c</span><span class="p">,</span> <span class="n">HEX</span><span class="p">);</span>
	<span class="n">Serial</span><span class="p">.</span><span class="n">println</span><span class="p">(</span><span class="s">""</span><span class="p">);</span>
  <span class="p">}</span>
<span class="p">}</span>

<span class="kt">void</span> <span class="nf">setup</span><span class="p">()</span> <span class="p">{</span>
	<span class="n">Serial</span><span class="p">.</span><span class="n">begin</span><span class="p">(</span><span class="mi">9600</span><span class="p">);</span>
	
	<span class="c1">// Initialize I2C communication</span>
	<span class="n">Serial</span><span class="p">.</span><span class="n">println</span><span class="p">(</span><span class="s">"Initializing I2C communication..."</span><span class="p">);</span>
	<span class="n">Wire</span><span class="p">.</span><span class="n">begin</span><span class="p">(</span><span class="n">I2C_ADDRESS</span><span class="p">);</span>
	<span class="n">Wire</span><span class="p">.</span><span class="n">onReceive</span><span class="p">(</span><span class="n">wireEvent</span><span class="p">);</span>
<span class="p">}</span>

<span class="kt">void</span> <span class="nf">loop</span><span class="p">()</span> <span class="p">{</span>

<span class="p">}</span>
</code></pre></div></div> <p>But measuring the signal with the oscilloscope, we just detected the beginning of the transaction, and no message is sent:</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/posts/I2C_Arduino/SCR11.PNG" sizes="95vw"/> <img src="/assets/posts/I2C_Arduino/SCR11.PNG" class="rounded" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption"> Without target node <code>0x1A</code> on the bus. </div> </div> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/posts/I2C_Arduino/SCR12.PNG" sizes="95vw"/> <img src="/assets/posts/I2C_Arduino/SCR12.PNG" class="rounded" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption"> With target node <code>0x1A</code> on the bus. </div> </div> </div> <p>This is because, <strong>without the acknowledgement from the target node, the controller does not send the message</strong>.</p> <p>Connecting both oscilloscope and reader node to the controller, we can indeed read the message with the arduino and see the right signal on the oscilloscope</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/posts/I2C_Arduino/oscilloscope_arduino_hello-480.webp 480w,/assets/posts/I2C_Arduino/oscilloscope_arduino_hello-800.webp 800w,/assets/posts/I2C_Arduino/oscilloscope_arduino_hello-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/posts/I2C_Arduino/oscilloscope_arduino_hello.jpg" class="rounded" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/posts/I2C_Arduino/SCR02.PNG" sizes="95vw"/> <img src="/assets/posts/I2C_Arduino/SCR02.PNG" class="rounded" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p>In the above picture, the data sent by the controller node is <code class="language-plaintext highlighter-rouge">Wire.write("Hello Oscilloscope");</code> and the oscilloscope is decoding the signal in ASCII instead of HEX.</p> <h3 id="on-the-structure-of-i2c-transactions">On the structure of I2C transactions</h3> <p>The structure of I2C transactions is reported in the left figure below; in this section I will compare this structure with the traces measured with the oscilloscope. For more details (still practical) on I2C protocol read <a href="https://docs.arduino.cc/learn/communication/wire/">here</a>.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="https://docs.arduino.cc/static/9d32a6e488d79f211470b15f06644a31/4ef49/I2C-480.webp 480w,https://docs.arduino.cc/static/9d32a6e488d79f211470b15f06644a31/4ef49/I2C-800.webp 800w,https://docs.arduino.cc/static/9d32a6e488d79f211470b15f06644a31/4ef49/I2C-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="https://docs.arduino.cc/static/9d32a6e488d79f211470b15f06644a31/4ef49/I2C.png" class="rounded" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption"> An I2C Message [source <a href="https://docs.arduino.cc/learn/communication/wire/" target="_blank">Arduino</a>] </div> </div> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/posts/I2C_Arduino/SCR08.PNG" sizes="95vw"/> <img src="/assets/posts/I2C_Arduino/SCR08.PNG" class="rounded" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption"> Signal recorded from oscilloscope </div> </div> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/posts/I2C_Arduino/SCR09.PNG" sizes="95vw"/> <img src="/assets/posts/I2C_Arduino/SCR09.PNG" class="rounded" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption"> Details </div> </div> </div> <h2 id="step-2-addressing-a-specific-register-subaddress">Step 2: Addressing a specific register subaddress</h2> <p>To get or set the state of the projector, I need to read or write (respectively) specific registers of its controller, as stated in the programmer’s guide. Each register address requires a certain number of data bytes, typically four. Thus, a register address is followed by variable length data. These bytes contain the value read or written into this register, with the most significant byte first.</p> <p>To move the read/write pointer head head to the correct address, the transaction stars with a write containing the register sub-address. The subaddress also contains a read/write bit in the most significant bit position. For read functions, bit 7 is set to 0. For write functions, bit 7 is set to 1.</p> <p>Therefore, each transaction (even those only comprising reading a register) starts in write mode, in order to send the subaddress to the target node. After START, 7bit address, and a 0bit for write mode, the subaddress byte is sent, pointing to the register containing the command of the desired DLPC350 function.</p> <p>If the controller wants to <strong>write</strong> on the selected register, the transaction proceeds with other data frames containing the data to be written. If in the other hand, the transaction is a <strong>read</strong>, the controller needs to send another I2C START condition followed by the target address with the I2C read/write bit set to 1.</p> <h3 id="read-get-projector-hardware-status">Read: <em>Get projector hardware status</em></h3> <p>Let us try to read the byte at address <code class="language-plaintext highlighter-rouge">0x20</code>, containing the hardware status of the projector, as stated in the table below. Looking at the trace from the oscilloscope, we can confirm that the projector initialized successfully and no error is detected.</p> <div class="language-c++ highlighter-rouge"><div class="highlight"><pre class="highlight"><code>
  <span class="c1">// Move reding pointer to register 0x20</span>
  <span class="n">Wire</span><span class="p">.</span><span class="n">beginTransmission</span><span class="p">(</span><span class="n">I2C_ADDRESS</span><span class="p">);</span> <span class="c1">// begin transmit to device 1</span>
  <span class="n">Wire</span><span class="p">.</span><span class="n">write</span><span class="p">(</span><span class="mh">0x20</span><span class="p">);</span> <span class="c1">// send register address </span>
  <span class="n">Wire</span><span class="p">.</span><span class="n">endTransmission</span><span class="p">();</span> <span class="c1">// end writing without sending stop bit</span>

  <span class="c1">// Read 2 bytes from register 0x20</span>
  <span class="n">Wire</span><span class="p">.</span><span class="n">requestFrom</span><span class="p">(</span><span class="n">I2C_ADDRESS</span><span class="p">,</span> <span class="mi">2</span><span class="p">);</span> <span class="c1">// request 1 byte from slave device</span>

</code></pre></div></div> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/posts/I2C_Arduino/hardware_status_table-480.webp 480w,/assets/posts/I2C_Arduino/hardware_status_table-800.webp 800w,/assets/posts/I2C_Arduino/hardware_status_table-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/posts/I2C_Arduino/hardware_status_table.png" class="rounded" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption"> Hardware Status Register </div> </div> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/posts/I2C_Arduino/SCR15.PNG" sizes="95vw"/> <img src="/assets/posts/I2C_Arduino/SCR15.PNG" class="rounded" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption"> Signal recorded from oscilloscope </div> </div> </div> <h3 id="write-turning-the-led-on-and-off">Write: <em>Turning the LED on and off</em></h3> <p>Finally we are at the point where we can turn the LED of the projector on and off, which is was essentially the first goal (we’ll see if we need more granular control for future photolithographic techniques).</p> <p>This is done by changing the content of register <code class="language-plaintext highlighter-rouge">0x10</code>, which contains the status od thre RBG LEDs. Actually our projector only has a blue LED, so the relevant bit to flip to turn it on and of is bit 2 of the register.</p> <div class="language-c++ highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">Wire</span><span class="p">.</span><span class="n">beginTransmission</span><span class="p">(</span><span class="n">I2C_ADDRESS</span><span class="p">);</span> <span class="c1">// begin transmit to device 1</span>
<span class="n">Wire</span><span class="p">.</span><span class="n">write</span><span class="p">(</span><span class="mh">0x90</span><span class="p">);</span> <span class="c1">// register 0x10 with bit 7 set to 1 for write mode</span>
<span class="n">Wire</span><span class="p">.</span><span class="n">write</span><span class="p">(</span><span class="mh">0x04</span><span class="p">);</span> <span class="c1">// turn on blue led</span>
<span class="c1">// Wire.write(0x00); // turn off blue led</span>
<span class="n">Wire</span><span class="p">.</span><span class="n">endTransmission</span><span class="p">();</span> <span class="c1">// end writing without sending stop bit</span>
</code></pre></div></div> <p>We can also change the intensity of the light by setting the PWM duty cycle of the LED power supply by setting the 3 <em>LED Driver Current Control</em> bytes at register <code class="language-plaintext highlighter-rouge">0x4B</code>.</p> <h3 id="setting-the-image">Setting the image</h3> <p>For the moment, the images to be projected are sent thought HDMI, but for the future I might need to look into individually addressing each micromirror to dynamically change the projected patterns.</p>]]></content><author><name></name></author><category term="project"/><category term="arduino"/><category term="microcontroller"/><category term="electronics"/><summary type="html"><![CDATA[Some notes on controlling a DLP projector with an Arduino through I2C protocol]]></summary></entry><entry><title type="html">Visual Studio 2019 for Fortran development on Windows</title><link href="https://arielsboiardi.eu/blog/2021/VS2019_fortran_dev/" rel="alternate" type="text/html" title="Visual Studio 2019 for Fortran development on Windows"/><published>2021-07-15T00:00:00+00:00</published><updated>2021-07-15T00:00:00+00:00</updated><id>https://arielsboiardi.eu/blog/2021/VS2019_fortran_dev</id><content type="html" xml:base="https://arielsboiardi.eu/blog/2021/VS2019_fortran_dev/"><![CDATA[<ul> <li><a href="#introduction">Introduction</a> <ul> <li><a href="#fortran-compilers">Fortran compilers</a></li> <li><a href="#the-visual-studio-ide">The Visual Studio IDE</a> <ul> <li><a href="#key-features"><strong>Key features</strong></a></li> <li><a href="#key-annoyances"><strong>Key annoyances</strong></a></li> </ul> </li> </ul> </li> <li><a href="#installation">Installation</a> <ul> <li><a href="#installing-visual-studio">Installing Visual Studio</a></li> <li><a href="#installing-intel-fortran">Installing Intel Fortran</a></li> <li><a href="#checking-the-configuration">Checking the configuration</a></li> </ul> </li> <li><a href="#general-guidelines-about-visual-studio-2019">General guidelines about Visual Studio 2019</a></li> <li><a href="#compiling-a-complex-code-library">Compiling a complex code library</a> <ul> <li><a href="#program-unit-and-subroutines-are-all-in-one-file"><strong>Program unit and subroutines are all in one file</strong></a></li> <li><a href="#code-with-modules-in-separate-files"><strong>Code with modules in separate files</strong></a></li> <li><a href="#code-with-modules-in-a-shared-library"><strong>Code with modules in a shared library</strong></a></li> <li><a href="#programs-exchanging-files"><strong>Programs exchanging files</strong></a></li> <li><a href="#compilation-of-parallel-code-using-mpi"><strong>Compilation of parallel code using MPI</strong></a></li> </ul> </li> <li><a href="#conclusion">Conclusion</a></li> </ul> <blockquote> <p><strong><em>NOTE:</em></strong> This guide was tested with the latest versions of the software at the time of writing, that is Visual Studio 2019 and Intel Fortran Compiler version 2021.3.0</p> </blockquote> <h2 id="introduction">Introduction</h2> <p>As of today Fortran is still one of the leading programming languages for high performance and scientific computing; on the other hand HPC and scientific computing are still a highly specialized niche and, as a result, not many user friendly tools are available for Fortran development.</p> <p>In this guide Visual Studio 2019 combined with the Intel Fortran compiler is proposed as a <em>do-it-all</em> Fortran IDE for Windows. On Linux many different, arguably better, alternatives are available.</p> <h3 id="fortran-compilers">Fortran compilers</h3> <p>Various Fortran compilers are available, a <a href="https://fortran-lang.org/learn/os_setup">comprehensive list</a> is available on the <a href="https://fortran-lang.org/">web site of the Fortran Programming Language</a>.</p> <p>I used the following two compilers</p> <ul> <li>GNU Fortran, or gfortran, part of the GNU Compiler Collection (GCC), is a Free and Open Source Fortran compiler which is still under active development and supports all the features of the latest Fortran 2018 standard. gfortran is natively available on Linux and MacOS; it is possible to get it on Windows with Windows Subsystem for Linux (WSL) and other workarounds.</li> <li>The Intel Fortran Compiler (ifort) is a proprietary compiler developed by Intel; it can be used free of charge for non commercial uses as a part of the Intel oneAPI HPC Toolkit. Intel Fortran is natively available on all platforms; but on Windows is virtually the only free option, thanks to the integration with Visual Studio.</li> </ul> <h3 id="the-visual-studio-ide">The Visual Studio IDE</h3> <p>Microsoft Visual Studio 2019 (VS for now on) is a complete and feature rich general purpose IDE; being general purpose it does not natively support Fortran development or other features which might be of interest in the context of scientific computing and, in particular, development of numerical analysis codes. Fortunately though the integration with Intel compiler and diagnostics tools makes it a viable option for our use case. VS is closed source and commercial software, but the <em>community edition</em> is free to use for non commercial purpose with a Microsoft account, the minimal functional limitation of this edition are not of any relevance for our use case.</p> <h4 id="key-features"><strong>Key features</strong></h4> <p>The key features that I found interesting are:</p> <ul> <li>Automatic determination of compilation order: on Linux I used the <a href="https://www.gnu.org/software/make/"><code class="language-plaintext highlighter-rouge">make</code></a> utility to compile and link the various components of my code; on Windows, due to my poor knowledge of its shell and lack of basic tools, I was not able to use <code class="language-plaintext highlighter-rouge">make</code>. This was the main reason I resorted to Visual Studio in the first place.</li> <li>Support for debugging, signally <ul> <li>Execution pause</li> <li>Break point support</li> <li>Variable inspector</li> </ul> </li> <li>Performance measurement <ul> <li>During execution CPU and memory usage are monitored and the runtime is tracked</li> <li>The IDE integrates with Intel performance diagnostics tools <code class="language-plaintext highlighter-rouge">VTune</code> adn <code class="language-plaintext highlighter-rouge">Advisor</code> <blockquote> <p>These tools are also available on Linux as separate applications, but on Windows they integrate in the IDE</p> </blockquote> </li> </ul> </li> </ul> <h4 id="key-annoyances"><strong>Key annoyances</strong></h4> <ul> <li>There is support for syntax highlighting, but absolutely no support for statement autocompletion</li> <li>There is no support for any kind of code exploration</li> <li>I suggest to write the code in a more feature rich text editor (<a href="https://fortran-lang.org/learn/os_setup/text_editors">here</a> are some very useful suggestions) and use VS only for compilation and debugging.</li> </ul> <h2 id="installation">Installation</h2> <p>The installation process involves the installation of VS and the Intel HPC Toolkit. It is important to follow the prescribed installation order to get out-of-the-box support.</p> <h3 id="installing-visual-studio">Installing Visual Studio</h3> <ul> <li>We need to download VS community edition from the page https://visualstudio.microsoft.com/it/</li> <li>Launch the installer and follow the direction <ul> <li>During the installation process we need to check the box of the <code class="language-plaintext highlighter-rouge">Desktop Development with C++</code> workload</li> <li>If you forget to check this box during the first install you can install the component from the <code class="language-plaintext highlighter-rouge">Visual Studio Installer</code> application that gets installed along VS.</li> </ul> </li> <li>At the end of the procedure, check that VS is installed on your system launching it. <ul> <li>On creating a new test project you should still not have options involving Fortran language…</li> </ul> </li> </ul> <h3 id="installing-intel-fortran">Installing Intel Fortran</h3> <ul> <li>The intel fortran compiler is part of the <a href="https://software.intel.com/content/www/us/en/develop/tools/oneapi/components/fortran-compiler.html">oneAPI HPC Toolkit</a>. On the <a href="https://software.intel.com/content/www/us/en/develop/tools/oneapi/hpc-toolkit/download.html?operatingsystem=window&amp;distributions=webdownload&amp;options=online">download page</a> we suggest to select an <em>Online</em> installer.</li> <li>Launch the installer and follow the directions: <ul> <li>Proceed with a default install (all components are installed)</li> <li>Be sure to check the box <em>Microsoft Visual Studio 2019</em> in the <em>Integrate IDE</em> step</li> </ul> </li> <li>At the end of the process close the installer.</li> </ul> <h3 id="checking-the-configuration">Checking the configuration</h3> <p>At this point creating a new project in VS an option for Fortran language should appear. We can test the configuration creating a simple `Hello World!” program:</p> <ul> <li>From the templates, select <code class="language-plaintext highlighter-rouge">Empty Project</code></li> <li>In the next screen give it a name and a directory to save it.</li> <li>Create a new file in the project: <ul> <li><code class="language-plaintext highlighter-rouge">Project &gt; Add New Item (Ctrl + Shift + A)</code></li> <li>Select the kind of Fortran file you want to create <blockquote> <p><strong>Keep in mind</strong> that there is no difference between these choices a part from the file extension, all files are simply plain text files. The extension is used by the IDE to chose what format, syntax highlighting or compiler options to use…</p> </blockquote> </li> <li>In the file we can create a program unit containing the following code : <div class="language-fortran highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">program</span><span class="w"> </span><span class="n">hello</span><span class="w"> 
        
    </span><span class="k">implicit</span><span class="w"> </span><span class="k">none</span><span class="w"> 

    </span><span class="k">write</span><span class="p">(</span><span class="o">*</span><span class="p">,</span><span class="o">*</span><span class="p">)</span><span class="w"> </span><span class="s2">"Hello World from Fortran compiled in Visual Studio 2019"</span><span class="w">
    </span><span class="k">pause</span><span class="w">

</span><span class="k">end</span><span class="w"> </span><span class="k">program</span><span class="w"> </span><span class="n">hello</span><span class="w">
</span></code></pre></div> </div> </li> </ul> </li> <li>To run the program press the <code class="language-plaintext highlighter-rouge">Start</code> in the toolbar, or press the <code class="language-plaintext highlighter-rouge">F5</code> function key</li> <li>If the program runs as expected Visual Studio 2019 should be configured correctly.</li> </ul> <h2 id="general-guidelines-about-visual-studio-2019">General guidelines about Visual Studio 2019</h2> <p>The way Visual Studio manages files, directories, projects, solutions, libraries…is quite messy, and gave me a headache at first, mainly because the file structure in VS does not reflect at all in the filesystem, which is quite uncanny from a Linux user perspective… So I try to summarize the main working concepts and jargon:</p> <ul> <li>The biggest units are <em>solutions</em>, which can contain many <em>projects</em></li> <li>A <em>project</em> is the basic unit and corresponds to a program or library <ul> <li>A <em>project</em> can contain at most one main program unit</li> <li>All source files for modules used by the main program can be contained in the <em>project</em></li> <li>Modules that we intend to use in more than one main program need to be compiled in a dedicated <em>project</em> <ul> <li>such <em>project</em> needs to be a <em>static library</em></li> <li>It is possible to create <code class="language-plaintext highlighter-rouge">dynamically-linked libraries</code> (<code class="language-plaintext highlighter-rouge">dll</code>s), but more configuration is necessary</li> <li>The dependency of the <em>project</em> containing the main program on the <em>project</em> containing the modules needs to be set in the <em>solution</em> properties to let the compiler know</li> </ul> </li> <li>All the compiler and runtime settings are specified on a <em>project</em> basis in the project properties</li> </ul> </li> <li>Each <em>project</em> can “contain” source files <ul> <li>Files can be created in the project as in the previous example with <code class="language-plaintext highlighter-rouge">Project &gt; Add New Item (Ctrl + Shift + A)</code></li> <li>or added from existing files <blockquote> <p><strong>Note that</strong></p> <ul> <li>all the directories and files in the <em>project</em> are <em>virtual</em>, that is they do not reflect their location in the filesystem (for some reason 🤔), so the source files can be kept all in a single directory and assigned to different project</li> <li>Similarly files that in the filesystem are in the same folder as the source files or the project will not appear in the project in VS</li> <li>The modifications to the files are, of course, reflected in the original files whatever their location in the filesystem</li> </ul> </blockquote> </li> </ul> </li> <li>When <code class="language-plaintext highlighter-rouge">▶Start</code> in the toolbar or the <code class="language-plaintext highlighter-rouge">F5</code> key is pressed, the project (along with its dependencies) is compiled and the mai program is executed <ul> <li>The <em>configuration</em> specifies the targets and platform <ul> <li>By default we have a <code class="language-plaintext highlighter-rouge">debug</code> and a <code class="language-plaintext highlighter-rouge">deploy</code> targets</li> <li>… and we can compile <code class="language-plaintext highlighter-rouge">x86</code> and <code class="language-plaintext highlighter-rouge">x64</code> Windows binaries</li> <li>The binaries are saved in a directory specified in the <em>project</em> properties <blockquote> <p><strong>Note that</strong> all setting in the project properties are relative to a single configuration, so pay attention what configuration is being used… I suggest selecting <code class="language-plaintext highlighter-rouge">All Configurations</code> when changing project properties</p> </blockquote> </li> </ul> </li> <li>The execution folder (working directory) is specified int he <em>project</em> properties</li> </ul> </li> </ul> <h2 id="compiling-a-complex-code-library">Compiling a complex code library</h2> <p>Here I illustrate how to get code compiled and running in Visual Studio 2019 through examples based on my experience with my IgA-based Energetic BEM library <a href="https://github.com/arielsboiardi/IgA-EnergeticBEM">IgA-EnergeticBEM</a>.</p> <p>In all the example we are located in a <em>solution</em> named, for example <code class="language-plaintext highlighter-rouge">IgA-EBEM</code></p> <h3 id="program-unit-and-subroutines-are-all-in-one-file"><strong>Program unit and subroutines are all in one file</strong></h3> <p>In this case the compilation is very simple: hitting <code class="language-plaintext highlighter-rouge">Start</code> should do it, if it does not there might be some error in the code.</p> <h3 id="code-with-modules-in-separate-files"><strong>Code with modules in separate files</strong></h3> <p>This case is again pretty simple: within the same project, Visual Studio automatically determines the dependencies and the right compile order. So we just press <code class="language-plaintext highlighter-rouge">F5</code></p> <h3 id="code-with-modules-in-a-shared-library"><strong>Code with modules in a shared library</strong></h3> <p>If we want the modules to be accessible to more than one main program, we can compile them in a <em>static library</em> and link it to the main program.</p> <ul> <li>Let us assume that the main program source file <code class="language-plaintext highlighter-rouge">IgaEBEM_2D_matrix.f90</code> is contained in a project named <code class="language-plaintext highlighter-rouge">matrix</code> and the modules are in a project named <code class="language-plaintext highlighter-rouge">lib_static</code> <ul> <li>We can check that the modules compile building just the <code class="language-plaintext highlighter-rouge">lib_static</code> project with the corresponding item of the context menu of the project.</li> </ul> </li> <li>In <code class="language-plaintext highlighter-rouge">Project &gt; Project Dependencies</code> we select <code class="language-plaintext highlighter-rouge">matrix</code> and in <code class="language-plaintext highlighter-rouge">Depends on</code> check <code class="language-plaintext highlighter-rouge">lib_static</code></li> </ul> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/posts/Fortran_VS2019/deps1-480.webp 480w,/assets/posts/Fortran_VS2019/deps1-800.webp 800w,/assets/posts/Fortran_VS2019/deps1-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/posts/Fortran_VS2019/deps1.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="Figure 1" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <ul> <li>We can now open the main program source file and press <code class="language-plaintext highlighter-rouge">Start</code> <ul> <li>In the output log we should see that all modules are being compiled</li> <li>After all the modules the main program gets compiled and executed</li> </ul> </li> </ul> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/posts/Fortran_VS2019/compilation_log-480.webp 480w,/assets/posts/Fortran_VS2019/compilation_log-800.webp 800w,/assets/posts/Fortran_VS2019/compilation_log-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/posts/Fortran_VS2019/compilation_log.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="Figure 1" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <ul> <li>We can now create other <em>projects</em> for other main program, and link them against the library with the same procedure <ul> <li>In our code library such programs programs would be <ul> <li><code class="language-plaintext highlighter-rouge">IgaEBEM_2D_rhs.f90</code>, for the computation of the right-hand side</li> <li><code class="language-plaintext highlighter-rouge">IgaEBEM_2D_solver.f90</code>, which implements the time marching solver.</li> </ul> </li> </ul> </li> </ul> <h3 id="programs-exchanging-files"><strong>Programs exchanging files</strong></h3> <ul> <li>Our programs are designed to exchange data saved on files, we therefore need to set the same working directory for all the projects tht contain a program unit. <ul> <li>To do so in each project properties under <code class="language-plaintext highlighter-rouge">Debugging</code> we set <code class="language-plaintext highlighter-rouge">Working Directory</code> to <code class="language-plaintext highlighter-rouge">$(SolutionDir)/run_dir</code>, so the programs are executed in a subdirectory <code class="language-plaintext highlighter-rouge">run_dir</code> of the directory containing all the solution.</li> </ul> </li> <li>The <code class="language-plaintext highlighter-rouge">contorno.dat</code> file and other files needed during the program runtime need to be in <code class="language-plaintext highlighter-rouge">run_dir</code></li> </ul> <h3 id="compilation-of-parallel-code-using-mpi"><strong>Compilation of parallel code using MPI</strong></h3> <p>To compile the code taking advantage of the parallel implementation</p> <ul> <li>In each project properties under <code class="language-plaintext highlighter-rouge">Configuration Properties &gt; Fortran &gt; Language &gt; Process OpenMP Directives</code> we select <code class="language-plaintext highlighter-rouge">Generate Parallel Code</code></li> </ul> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/posts/Fortran_VS2019/qomp-480.webp 480w,/assets/posts/Fortran_VS2019/qomp-800.webp 800w,/assets/posts/Fortran_VS2019/qomp-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/posts/Fortran_VS2019/qomp.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="Figure 1" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <ul> <li><strong>Note that</strong> also the library needs to be compiled with the <code class="language-plaintext highlighter-rouge">\Qopenmp</code> flag</li> </ul> <h2 id="conclusion">Conclusion</h2> <p>In this article I <del>hopefully</del> gave a complete introduction to the usage of Visual Studio 2019 and Intel Fortran Compiler aimed at numerical analysts and Fortran developers who need or want to work on Windows.</p> <p>I will continue to do my coding on Linux, as more powerful tools are freely available, many HPC centers, including the one at my University, ase Linux systems, and I have more control on what I am doing, instead of needing to look for mysterious options and learning funny file structures with their own lexicon.</p>]]></content><author><name></name></author><category term="tutorial"/><category term="guide"/><category term="fortran"/><category term="coding"/><category term="numerics"/><summary type="html"><![CDATA[A guide to Fortran development on Windows]]></summary></entry></feed>